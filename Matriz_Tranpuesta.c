#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main () {

    int a,b;
    printf ("Ingrese el valor de la fila de la matriz: ")
    scanf ("%d", &a);
    printf ("Ingrese el valor de la columna de la matriz: ");
    scanf ("%d", &b);


    return 0;
}